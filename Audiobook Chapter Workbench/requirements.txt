# PyTorch CUDA Index for Windows GPU support
--extra-index-url https://download.pytorch.org/whl/cu121

# Core PyTorch ecosystem (pinned for stability with CUDA 12.1)
torch==2.5.1
torchaudio==2.5.1

# NVIDIA cuDNN v8 DLLs (Fixes missing cudnn_ops_infer64_8.dll on Windows)
nvidia-cudnn-cu11==8.9.7.29

# Transcription & Audio Engines
whisperx
faster-whisper

# Utilities
natsort